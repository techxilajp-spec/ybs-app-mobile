export type BusStop = {
  id: string | number;
  lat: string | number;
  lng: string | number;
  name_en: string;
  name_mm: string;
  road_en: string,
  road_mm: string,
  township_en: string,
  township_mm: string
};
